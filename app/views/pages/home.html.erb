<div class="store-index-div">
  <% if @current_user.present? %>
    <% @current_user.stores.each do |store| %>
      <h1 class="store-index-text"><%= link_to(store.name, store_path(store.id)) %></h1>
      <h2>Store API Key: <%= store.api_key %></h2>
      <br>
      <h2 class="store-index-text"><%= store.description %></h2>
    <% end %>
    <% else %>
  <div class="hero">
    <div class="container">
       <h1>Welcome</h1>
       <p>The Backend is built as a customizable API. It collects and saves all your data into this backend app and gives you access to monitor and manage your online e-commerce store. It is a one stop solution for the back end and sends your data to any front-end site with an AJAX request through your own API key.</p>
       <br>
       <p>It is currently a working MVP and is still a work in progess.</p>
    </div>

  </div>

  <% end %>
</div>
